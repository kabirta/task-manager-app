<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white min-h-screen">
    <div class="main w-full min-h-screen flex flex-col items-center py-8 px-4">
        <h1 class="text-4xl font-bold mb-8">My Tasks</h1>

        <form action="/create-task" method="POST" class="w-full max-w-lg mb-12">
            <div class="mb-4">
                <input type="text" name="title" class="w-full p-3 bg-zinc-800 border border-zinc-700 rounded-md placeholder-zinc-400 focus:outline-none focus:ring-2 focus:ring-blue-500" placeholder="Title goes here..." required>
            </div>
            <div class="mb-4">
                <textarea name="details" class="w-full p-3 bg-zinc-800 border border-zinc-700 rounded-md placeholder-zinc-400 focus:outline-none focus:ring-2 focus:ring-blue-500" placeholder="Write your task details here..."></textarea>
            </div>
            <button type="submit" class="w-full bg-blue-600 hover:bg-blue-700 text-white font-semibold py-3 rounded-md transition duration-200">Create Task</button>
        </form>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 w-full max-w-4xl">
            <% if (files && files.length > 0) { %>
                <% files.forEach(function(file) { %>
                    <div class="bg-zinc-800 p-6 rounded-lg shadow-lg">
                        <h3 class="text-xl font-semibold mb-2"><%= file %></h3>
                        <a href="/read/<%= file %>" class="text-blue-400 hover:underline">Read More.</a>
                        <a class="text-zinc-600" href="/edit/<%=file%>" >edit</a>
                        

                    </div>
                <% }); %>
            <% } else { %>
                <p class="col-span-full text-center text-zinc-400">No tasks yet. Create one above!</p>
            <% } %>
        </div>
    </div>
</body>
</html>