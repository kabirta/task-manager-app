<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Task Details</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white min-h-screen flex flex-col items-center py-8 px-4">
    <div class="w-full max-w-2xl bg-zinc-800 p-8 rounded-lg shadow-lg">
        <h1 class="text-3xl font-bold mb-4 text-blue-400"><%= title %></h1>
        <div class="prose prose-invert max-w-none text-zinc-300 whitespace-pre-wrap">
            <p><%= details %></p>
        </div>
        <a href="/" class="mt-6 inline-block bg-blue-600 hover:bg-blue-700 text-white font-semibold py-2 px-4 rounded-md transition duration-200">Back to Tasks</a>
    </div>
</body>
</html>